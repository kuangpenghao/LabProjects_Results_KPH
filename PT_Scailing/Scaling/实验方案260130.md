## 1. BERT Baseline
训练4个或5个（Large可能暂时不做）不同大小的bert模型作为参考。
| 模型名称 | 层数  | 隐藏层维度  | 注意力头数  | FFN 中间层维度 | 参数量 |
|----------|--------|----------------|----------------|------------------|----------------------|
| BERT-Tiny     | 2  | 128  | 2  | 512  | 4.4M  |
| BERT-Mini     | 4  | 256  | 4  | 1024 | 11.3M |
| BERT-Medium   | 8  | 512  | 8  | 2048 | 42.0M |
| BERT-Base     | 12 | 768  | 12 | 3072 | 110M  |
| BERT-Large    | 24 | 1024 | 16 | 4096 | 340M  |

## 2. PT小模型调参

在`dim_z=256`，`ternary rank=64`，`num channels=10`的小模型上调参。

待调参数：

* binary factor scaling
* ternary factor scaling
* classifier amplifier
* regularize_z
* regularize_g
* regularize_h
* learning rate

固定参数：
* dim_g/dim_z=4
* num_iteration=6
* potential func z=square
* potential func g=abs

## 3. num channels缩放

尝试dim_z=256（已在调参阶段训练完毕） 512 1024 1536 2560的模型，且ternary rank不变，num channels随dim_z线性缩放

* 512
  * rank=64
  * channel=20
* 1024
  * rank=64
  * channel=40
* 1536
  * rank=64
  * channel=60
* 2560
  * rank=64
  * channel=100

## 4. ternary rank缩放

尝试dim_z=256（已在调参阶段训练完毕） 512 1024 1536 2560的模型，且num channels不变，ternary rank随dim_z线性缩放

* 512
  * rank=128
  * channel=10
* 1024
  * rank=256
  * channel=10
* 1536
  * rank=384
  * channel=10
* 2560
  * rank=640
  * channel=10

## 5. AdaLN

一定程度上实现各层的解绑定。

算法介绍：

**参数**:
*   `Shared Weights`: $W_u, W_v, W_{topic}$ (所有层共享)
*   `AdaLN Params`: $\Gamma \in \mathbb{R}^{L \times N}, \text{B} \in \mathbb{R}^{L \times N}$ ($L$为迭代次数, $N$为dim\_z) 

1.  初始化 $qz_0 = \text{unary\_potentials}$
2.  循环 $t = 0 \to L-1$:
    *   **提取当前层的调制参数**:
        $\gamma_t = \Gamma[t]$, $\beta_t = \text{B}[t]$。即：每层（每个迭代步）拥有独立的$\gamma$和$\beta$
    *   **归一化 (保持原逻辑 + Energy Recovery)**:
        $z_{norm} = \text{Norm}(qz_t) \times \text{dim\_z}$
    *   **[唯一修改步骤] AdaLN 调制**:
        $z_{mod} = z_{norm} \cdot (1 + \gamma_t) + \beta_t$
    *   **计算功能流 (使用调制后的 $z_{mod}$)**:
        $m_1 = \text{HeadSelection}(z_{mod}, W_u, W_v)$
        $m_2 = \text{TopicModeling}(z_{mod}, W_{topic})$
    *   **状态更新 (Structural Update)**:
        $qz_{t+1} = (m_1 + m_2 + \text{unary}) / \text{regularize\_z}$
        $qz_{t+1} = \text{Damp}(qz_{t+1}, qz_t)$
3.  输出 $qz_L$

实验方案：

超参数不变。先试验dim_z=256时与不使用相比的效果。若有效，则可尝试该方法与num channels缩放、ternary rank缩放叠加作用的效果。